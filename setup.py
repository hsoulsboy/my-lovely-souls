from setuptools import setup, find_packages

setup(
    name='steamSearcher',
    version='1.0.0',
    author='hsoulsboy',
    packages=find_packages(include=['steamSearcher', 'steamSearcher.*'])
)